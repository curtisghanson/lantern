{% extends 'LanternAdminBundle::base.html.twig' %}

{% set pageIcon = 'group' %}
{% set pageTitle = 'admin.panel'|trans %}
{% set pageSubtitle = 'view.group'|trans %}
{% set activeItem = 'LanternAuthBundle_group_index' %}

{% block navBody %}
    {% include 'LanternAdminBundle::nav.html.twig' %}
{% endblock navBody %}

{% block contentBody -%}
    <h1>Group</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Shortname</th>
                <td>{{ entity.shortName }}</td>
            </tr>
            <tr>
                <th>Longname</th>
                <td>{{ entity.longName }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ entity.description }}</td>
            </tr>
            <tr>
                <th>Isactive</th>
                <td>{{ entity.isActive }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('LanternAuthBundle_group_index') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('LanternAuthBundle_group_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock contentBody %}
